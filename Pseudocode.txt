var
    NamaBarang, Keterangan : string;
    JumlahBarang, HargaBarang, TotalHarga : integer;

begin
    Main()
        Input (NamaBarang);
        Input (JumlahBarang);
        Input (HargaBarang);
        Input (Keterangan);

        if Keterangan == "Belum Selesai"
        begin
            Main()
        end;

end.